json.extract! family, :id, :main_parent, :sec_parent, :last_name, :street, :city, :state, :zip, :phone1, :phone2, :phone3, :email, :note, :created_at, :updated_at
json.url family_url(family, format: :json)
